# Agent instructions

Read [AI_CONTEXT.md](AI_CONTEXT.md) before working in this repository. It defines the commands, technical boundaries, source-of-truth rules, and approval gates.

`SECOND_BRAIN_PATH` may enable optional local context through a hook. It is never required and must not be written into repository files.
